name: exp-test-benchmark
dir: runs

training:
  test_size: 0.3
  epochs: 100
  criterion: 'cross-entropy'

parameters:
  model: 'benchmark'
  optimizer: 'adam'
  learning_rate: 0.01

# Model parameters
  model_params:
    layers: [
      10,
      10
    ]
    activation_name: 'relu'
    activation_out: 'relu'